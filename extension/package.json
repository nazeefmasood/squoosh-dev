{
  "name": "smoosh-ext",
  "version": "0.1.0",
  "private": true,
  "description": "Remove Gemini watermarks and compress AI-generated images right in the chat.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
